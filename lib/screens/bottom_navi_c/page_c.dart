
import 'package:flutter/material.dart';

class BottomCPageC extends StatelessWidget {
  const BottomCPageC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BottomC PageC'),
      ),
      body: const Center(child: Text('BottomC PageC')),
    );
  }
}
