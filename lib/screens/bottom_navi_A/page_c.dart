import 'package:flutter/material.dart';

class BottomAPageC extends StatelessWidget {
  const BottomAPageC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BottomA PageC'),
      ),
      body: const Center(child: Text('BottomA PageC')),
    );
  }
}
