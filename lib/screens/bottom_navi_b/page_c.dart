import 'package:flutter/material.dart';

class BottomBPageC extends StatelessWidget {
  const BottomBPageC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BottomB PageC'),
      ),
      body: const Center(child: Text('BottomB PageC')),
    );
  }
}
